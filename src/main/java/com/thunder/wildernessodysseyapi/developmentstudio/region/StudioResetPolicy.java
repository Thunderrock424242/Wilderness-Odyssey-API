package com.thunder.wildernessodysseyapi.developmentstudio.region;

/** Declares the only reset behavior a registered Studio test region permits. */
public enum StudioResetPolicy {
    NONE,
    BLOCK_SNAPSHOT,
    TAGGED_ENTITIES
}
