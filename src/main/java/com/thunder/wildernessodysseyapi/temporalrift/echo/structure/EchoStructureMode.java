package com.thunder.wildernessodysseyapi.temporalrift.echo.structure;

/** Relationship of an authored structure set to the two Earth's independent histories. */
public enum EchoStructureMode {
    SHARED,
    EARTH_ONLY,
    ECHO_ONLY,
    ECHO_VARIANT
}
