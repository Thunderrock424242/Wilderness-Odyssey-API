package com.thunder.wildernessodysseyapi.performance.background;

/** Describes how immediately a region or object matters to current players. */
public enum ActivityLevel {
    ACTIVE,
    NEARBY,
    BACKGROUND,
    DORMANT
}
